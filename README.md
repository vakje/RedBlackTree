# Red Black Tree 
## Overview
This is structure implemenation. 

### Built with
- C++

### Features

- push data into the tree which will validate the tree rotate it adjust it automatically 
- Display Data (only Preorder)
- Delete Data recursively 
- search  Preorder  Postorder 
- copy trees, perform deepcopy with recursion in  assignment operator and copy costructor
